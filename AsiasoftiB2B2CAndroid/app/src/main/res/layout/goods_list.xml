<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="#fffafafa"
    android:orientation="vertical" >

    <ListView
        android:id="@id/goodsListView"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:cacheColorHint="#fff5f5f5"
        android:divider="@drawable/cart_listview_gap"
        android:fadingEdge="none"
        android:listSelector="#fff5f5f5"
        android:background="#F1F5FB" />

    <TextView
        android:id="@id/textNoNoDatas"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:drawableLeft="@drawable/notified"
        android:text="没有数据"
        android:textColor="#ff6d6d6d"
        android:textSize="20.0dip"
        android:visibility="gone" />

</RelativeLayout>